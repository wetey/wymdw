import streamlit as st

st.set_page_config(page_title="Summary")

st.markdown("""
    
    We clustered the errors so the semantically similar examples are grouped together. We looked at the error type distribution per cluster. Lastly, we looked at the top words per error type. 
            
    With these visualization we were able to gain a better understanding of what the model is misinterpreting. 


""")